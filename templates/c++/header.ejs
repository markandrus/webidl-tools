#ifndef <%- headerDefine(node) %>
#define <%- headerDefine(node) %>

#include "nan.h"
#include "v8.h"

namespace <%- namespace -%> {

<%_ if (node.type === 'dictionary') { _%>
<%-   include({ template: './dictionary.h.ejs' }); %>
<%_ } else if (node.type === 'enum') { _%>
<%-   include({ template: './enum.h.ejs' }); %>
<%_ } _%>

}  // namespace <%- namespace %>

#endif  // <%- headerDefine(node) %>

