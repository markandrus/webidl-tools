enum <%- node.name %> {
<%_ node.values.forEach((value, i) => { _%>
<%- '  k' + cppEnum(value) + (i < node.values.length - 1 ? ',' : '') %>
<%_ }) _%>
};

v8::Local<v8::Value> <%- node.name %>ToV8(<%- node.name %> parsed);

Nan::Maybe<<%- node.name %>> <%- node.name %>FromV8(v8::Local<v8::Value> unparsed, std::string *error);
