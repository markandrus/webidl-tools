enum <%- enumName(node) %> {
<%_ node.values.forEach((value, i) => { _%>
<%- '  ' + toEnumName(node.name, value) + (i < node.values.length - 1 ? ',' : '') %>
<%_ }) _%>
};

v8::Local<v8::Value> <%- enumName(node) %>ToV8(<%- enumName(node) %> parsed);

Nan::Maybe<<%- enumName(node) %>> <%- enumName(node) %>FromV8(v8::Local<v8::Value> unparsed, std::string *error);
