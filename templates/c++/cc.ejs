#include "<%- headerName(node) %>"

<%_ if (node.type === 'dictionary') { _%>
<%-   include({ template: './dictionary.cc.ejs' }); %>
<%_ } else if (node.type === 'enum') { _%>
<%-   include({ template: './enum.cc.ejs' }); %>
<%_ } _%>

