type <%= node.name %> = {
<%_ node.members.forEach((member, i) => { _%>
  <%- include({ member: member, template: './attribute.ejs' }) -%><%= i === node.members.length - 1 ? '' : ',' %>
<%_ }) _%>
};
