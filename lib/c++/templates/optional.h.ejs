#ifndef <%- headerDefine('optional') %>
#define <%- headerDefine('optional') %>

#include "webrtc/base/optional.h"

namespace <%- namespace %> {

// This project targets C++11; hence we cannot use std::optional, which is only
// available in C++17. While we _could_ define our own implementation, this
// project is primarily being used with WebRTC bindings where rtc::Optional is
// already available, so let us simply define a typedef for WebRTC's Optional
// implementation.
template <typename T>
using Optional = rtc::Optional<T>;

}  // namespace <%- namespace %>

#endif  // <%- headerDefine('optional') %>
