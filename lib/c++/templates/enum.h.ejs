enum <%- enumName(node) %> {
<%_ node.values.forEach((value, i) => { _%>
<%- '  ' + toEnumName(node.name, value) + (i < node.values.length - 1 ? ',' : '') %>
<%_ }) _%>
};

template <>
struct Marshal<<%- enumName(node) %>> {
  static rtc::Optional<<%- enumName(node) %>> FromV8(v8::Local<v8::Value> value, std::string *error);

  static v8::Local<v8::Value> ToV8(<%- enumName(node) %> value);
};
